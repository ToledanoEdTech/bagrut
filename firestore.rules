rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All data access goes through Next.js API (Firebase Admin SDK).
    // Client-side direct access is disabled.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
